[user]
  name = Juan C. Müller
  email = jcmuller@gmail.com
[core]
  excludesfile = /home/jcmuller/.gitignore
  quotepath = false
[difftool]
  prompt = false
[diff]
  tool = vimdiff2
[merge]
  tool = vimdiff2
[mergetool]
  prompt = false
[help]
  autocorrect = 1
[color]
  ui = true
[github]
  user = jcmuller
  password = !security find-internet-password -a jcmuller -s github.com -w | tr -d '\n'
  token = TOKEN
  prq-token = TOKEN
[url "git@github.com:"]
  insteadOf = "gh:"
[push]
  default = current
[init]
  templatedir = /home/jcmuller/.git_template
[branch]
  autosetuprebase = always
#[remote "origin"]
  #fetch = +refs/pull/*/head:refs/remotes/origin/pr/*
[alias]
  co = checkout
  ci = commit
  local-ignore = update-index --assume-unchanged
  local-unignore = update-index --no-assume-unchanged
  local-ignored = !git ls-files -v | grep "^[[:lower:]]"
[rerere]
  enabled = true
[credential]
	helper = cache --timeout=360000
