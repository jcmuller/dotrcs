[user]
  name = Juan C. Müller
  email = jcmuller@gmail.com
[core]
  excludesfile = /home/jcmuller/.gitignore
  quotepath = false
[difftool]
  prompt = false
[diff]
  tool = vimdiff3
[merge]
  tool = vimdiff3
[mergetool]
  prompt = false
[help]
  autocorrect = 1
[color]
  ui = true
[github]
  user = jcmuller
  password = !security find-internet-password -a jcmuller -s github.com -w | tr -d '\n'
  token = TOKEN
  prq-token = TOKEN
[url "git@github.com:"]
  insteadOf = https://github.com/
  insteadOf = gh:
[push]
  default = current
[init]
  templatedir = /home/jcmuller/.git_template
[branch]
  autosetuprebase = always
#[remote "origin"]
  #fetch = +refs/pull/*/head:refs/remotes/origin/pr/*
[alias]
  ci = commit
  co = checkout
  cor = !git-jump-to
  local-ignore = update-index --assume-unchanged
  local-ignored = !git ls-files -v | grep "^[[:lower:]]"
  local-unignore = update-index --no-assume-unchanged
  show-ignored = ls-files --exclude-standard --others --ignored
[rerere]
  enabled = true
[credential]
  helper = cache --timeout=360000
[fetch]
  prune = true
[pull]
  rebase = true
[rebase]
  autosquash = true
# diff.gpg.binary=true
# diff.gpg.textconv=gpg2 -d --quiet --yes --compress-algo=none --no-encrypt-to --batch --use-agent

